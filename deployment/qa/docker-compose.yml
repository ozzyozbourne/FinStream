services:
  frontend-qa:
    image: ozzydoc/finstream-frontend:qa
    container_name: finstream-frontend-qa
    restart: unless-stopped
    ports:
      - ${FRONTEND_QA_PORT}:80
    networks:
      - qa

networks:
  qa:
    external: true
